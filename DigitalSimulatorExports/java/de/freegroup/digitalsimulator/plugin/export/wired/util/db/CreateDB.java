package de.freegroup.digitalsimulator.plugin.export.wired.util.db;

/**
 * short-i Class generated by automatic ClassGenerator
 * Date: 2004-01-22
 */
import java.util.ArrayList;
import java.util.Iterator;
import java.sql.*;

import de.freegroup.digitalsimulator.db.*;

public class CreateDB { 
	public static void main ( String[] args )
	{
		try
		{
			SaveConnection conn=ConnectionManager.getValid();
			SaveStatement  stat=conn.createStatement("createDB");
		try {
			stat.executeUpdate("DROP TABLE Connection");
		} catch(SQLException e) {}// Ignore Exception, because the table may not yet exist
		stat.execute("CREATE TABLE Connection ("+
			"toRelatedObject_id  VARCHAR(50)  NOT NULL  , "+
			"fromPinNuum  int  NOT NULL  , "+
			"toPinNum  int  NOT NULL  , "+
			"fromRelatedObject_id  VARCHAR(50)  NOT NULL  , "+
			"id  VARCHAR(50)  NOT NULL  , "+
			"PRIMARY KEY    (id))");
		try {
			stat.executeUpdate("DROP TABLE ObjectPin");
		} catch(SQLException e) {}// Ignore Exception, because the table may not yet exist
		stat.execute("CREATE TABLE ObjectPin ("+
			"casePin_id  VARCHAR(50)  NOT NULL  , "+
			"object2Case_id  VARCHAR(50)  NOT NULL  , "+
			"description  VARCHAR(50)  NOT NULL  , "+
			"id  VARCHAR(50)  NOT NULL  , "+
			"number  bigint  NOT NULL  , "+
			"PRIMARY KEY    (id))");
		try {
			stat.executeUpdate("DROP TABLE ElectricCase");
		} catch(SQLException e) {}// Ignore Exception, because the table may not yet exist
		stat.execute("CREATE TABLE ElectricCase ("+
			"width  int  NOT NULL  , "+
			"height  int  NOT NULL  , "+
			"caseLayout_id  VARCHAR(50)  NOT NULL  , "+
			"name  VARCHAR(50)  NOT NULL  , "+
			"descriptionURL  VARCHAR(50)  NOT NULL  , "+
			"id  VARCHAR(50)  NOT NULL  , "+
			"iconName  VARCHAR(50)  NOT NULL  , "+
			"PRIMARY KEY    (id))");
		try {
			stat.executeUpdate("DROP TABLE CasePin");
		} catch(SQLException e) {}// Ignore Exception, because the table may not yet exist
		stat.execute("CREATE TABLE CasePin ("+
			"description  VARCHAR(50)  NOT NULL  , "+
			"y  bigint  NOT NULL  , "+
			"caseLayout_id  VARCHAR(50)  NOT NULL  , "+
			"x  bigint  NOT NULL  , "+
			"id  VARCHAR(50)  NOT NULL  , "+
			"number  bigint  NOT NULL  , "+
			"PRIMARY KEY    (id))");
		try {
			stat.executeUpdate("DROP TABLE ElectricObject");
		} catch(SQLException e) {}// Ignore Exception, because the table may not yet exist
		stat.execute("CREATE TABLE ElectricObject ("+
			"isSingelton  int  NOT NULL  , "+
			"type  VARCHAR(50)  NOT NULL  , "+
			"id  VARCHAR(50)  NOT NULL  , "+
			"PRIMARY KEY    (id))");
		try {
			stat.executeUpdate("DROP TABLE CaseLayout");
		} catch(SQLException e) {}// Ignore Exception, because the table may not yet exist
		stat.execute("CREATE TABLE CaseLayout ("+
			"id  VARCHAR(50)  NOT NULL  , "+
			"PRIMARY KEY    (id))");
		try {
			stat.executeUpdate("DROP TABLE Object2Case");
		} catch(SQLException e) {}// Ignore Exception, because the table may not yet exist
		stat.execute("CREATE TABLE Object2Case ("+
			"object_id  VARCHAR(50)  NOT NULL  , "+
			"electricCase_id  VARCHAR(50)  NOT NULL  , "+
			"id  VARCHAR(50)  NOT NULL  , "+
			"PRIMARY KEY    (id))");
		try {
			stat.executeUpdate("DROP TABLE MacroPinMapping");
		} catch(SQLException e) {}// Ignore Exception, because the table may not yet exist
		stat.execute("CREATE TABLE MacroPinMapping ("+
			"electricObjectPinNum  bigint  NOT NULL  , "+
			"relatedObjectPinNum  bigint  NOT NULL  , "+
			"electricObject_id  VARCHAR(50)  NOT NULL  , "+
			"id  VARCHAR(50)  NOT NULL  , "+
			"relatedObject_id  VARCHAR(50)  NOT NULL  , "+
			"PRIMARY KEY    (id))");
		try {
			stat.executeUpdate("DROP TABLE RelatedObject");
		} catch(SQLException e) {}// Ignore Exception, because the table may not yet exist
		stat.execute("CREATE TABLE RelatedObject ("+
			"masterObject_id  VARCHAR(50)  NOT NULL  , "+
			"id  VARCHAR(50)  NOT NULL  , "+
			"relatedObject_id  VARCHAR(50)  NOT NULL  , "+
			"PRIMARY KEY    (id))");
		try {
			stat.executeUpdate("DROP TABLE DependencyPin");
		} catch(SQLException e) {}// Ignore Exception, because the table may not yet exist
		stat.execute("CREATE TABLE DependencyPin ("+
			"objectPin_id  VARCHAR(50)  NOT NULL  , "+
			"casePin_id  VARCHAR(50)  NOT NULL  , "+
			"caseDependency_id  VARCHAR(50)  NOT NULL  , "+
			"id  VARCHAR(50)  NOT NULL  , "+
			"PRIMARY KEY    (id))");
		try {
			stat.executeUpdate("DROP TABLE CaseDependency");
		} catch(SQLException e) {}// Ignore Exception, because the table may not yet exist
		stat.execute("CREATE TABLE CaseDependency ("+
			"object_id  VARCHAR(50)  NOT NULL  , "+
			"electricCase_id  VARCHAR(50)  NOT NULL  , "+
			"id  VARCHAR(50)  NOT NULL  , "+
			"PRIMARY KEY    (id))");
			stat.close();
		}
		catch(Exception exc)
		{
			System.out.println(exc);
			exc.printStackTrace();
		}
	}
}
